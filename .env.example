# Nur für lokales Entwickeln/Experimentieren.
# Im echten Build wird der OpenAI-Key über sichere Speicherung abgelegt
# (Windows Credential Manager / DPAPI via Electron safeStorage), NICHT über .env.
#
# Benötigt Zugriff auf: whisper-1 (Transkription), gpt-4o-mini / gpt-4o (Umschreiben).
OPENAI_API_KEY=
